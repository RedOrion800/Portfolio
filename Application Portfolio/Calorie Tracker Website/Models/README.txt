The Userinfo.cs model is the model used to add records to the database with a single value of height.
The UserinfoInput.cs model is an identical model aside from it being feet and inches instead of height.
The feet and inches in converted into inches which is stored in the height of the Userinfo model then added to the database.
The information retrieved from the database is converted back to feet and inches when being displayed to the user.